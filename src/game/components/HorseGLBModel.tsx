/**
 * HorseGLBModel — placeholder until new horse GLB files are provided.
 * Renders nothing. Will be updated once standing + walking GLBs are uploaded.
 */

interface Props {
  moveSpeed: number | React.RefObject<number>;
  scale?: number;
  renderPath?: string;
}

export function HorseGLBModel({ }: Props) {
  return null;
}
